import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';

<Meta title="알림 기능/Alarm (Alert, Confirm)" />

# Alert, Confirm
공통 Alert, Confirm 컴포넌트 예제 코드입니다.

## Link
<a href="http://beedocs.mediapass.kr:4189/__example__/notification/alert" target="_blank" rel="noopener noreferrer">
  http://beedocs.mediapass.kr:4189/__example__/notification/alert
</a>

## Code
```jsx
/**
 * @file AlertConfirmTestPage.tsx
 * @description 간단한 이름을 받아 출력하는 컴포넌트
 */

import { useAlarm } from '~/utils/hooks/common/useAlarm'

const AlertConfirmTestPage = () => {
  const { setAlert, setConfirm } = useAlarm()

  const handleAlert = () => {
    setAlert({
      message: <p>헬로 월드</p>,
      open: true,
      callback: () => {
        alert('알림 확인')
      },
    })
  }

  const handleConfirm = () => {
    setConfirm({
      message: <p>헬로 컨펌</p>,
      open: true,
      onCancel: () => {
        alert('컨펌 취소')
      },
      onConfirm: () => {
        alert('컨펌 확인')
      },
    })
  }

  return (
    <div style={{ margin: '30px' }}>
      <button
        type="button"
        onClick={handleAlert}
        style={{ border: '1px solid #333', padding: '10px' }}
      >
        Alert
      </button>
      <br />
      <br />
      <button
        type="button"
        onClick={handleConfirm}
        style={{ border: '1px solid #333', padding: '10px' }}
      >
        Confirm
      </button>
    </div>
  )
}

export default AlertConfirmTestPage

```
